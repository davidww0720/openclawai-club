name = "openclawai-club"
compatibility_date = "2026-02-12"

# OpenNext requires Node.js built-ins on Workers.
compatibility_flags = ["nodejs_compat"]

routes = [
  { pattern = "openclawai.club/*", zone_name = "openclawai.club" }
]

[vars]
NODE_VERSION = "22"
